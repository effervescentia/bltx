import { createThemeContract } from '@vanilla-extract/css';

export const themeContract = createThemeContract({
  space: {
    none: null,
    small: null,
    medium: null,
    large: null,
  },
});
